services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - clips:/app/clips
      - data:/app/data
    environment:
      - DB_PATH=/app/data/motion_logs.db

volumes:
  clips:
  data:
